apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myKubeAppChart.fullname" . }}-test-connection"
  labels:
    {{- include "myKubeAppChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myKubeAppChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
