apiVersion: v1
kind: Service
metadata:
  name: {{ include "myKubeAppChart.fullname" . }}
  labels:
    {{- include "myKubeAppChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myKubeAppChart.selectorLabels" . | nindent 4 }}
